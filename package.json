{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node dev-server.js",
    "start:dev": "nodemon --config nodemon.json",
    "lint": "npm run lint --workspaces && standard .",
    "lint:fix": "npm run lint:fix --workspaces && standard --fix .",
    "test": "npm test --workspaces"
  },
  "name": "@bryopsida/node-red-onvif",
  "devDependencies": {
    "@node-red-contrib-themes/theme-collection": "^4.1.0",
    "lerna": "^8.2.0",
    "node-red": "^4.1.0",
    "nodemon": "^3.1.10",
    "standard": "^17.1.2"
  }
}
